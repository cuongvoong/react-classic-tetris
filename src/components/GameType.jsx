import React from "react";

const GameType = props => {
  return (
    <section className="game-type">
      <h2 className="game-type-header">GAME TYPE</h2>
      <div className="a-type">A-TYPE</div>
      <div className="b-type">B-TYPE</div>
    </section>
  );
};

export default GameType;
